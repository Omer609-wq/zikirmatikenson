<?xml version="1.0" encoding="utf-8" ?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <application
        android:allowBackup="false"
        android:fullBackupContent="@xml/backup_rules"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:enableOnBackInvokedCallback="true"
        android:theme="@style/AppTheme">
        <activity
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|locale|smallestScreenSize|screenLayout|uiMode|navigation"
            android:name="com.omerzikirmatik.app.MainActivity"
            android:label="@string/title_activity_main"
            android:theme="@style/AppTheme.NoActionBarLaunch"
            android:launchMode="singleTask"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data android:name="android.support.FILE_PROVIDER_PATHS" android:resource="@xml/file_paths" />
        </provider>
    </application>

    <!-- Permissions -->

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.VIBRATE" />
    <!-- Android 13+ (API 33): bildirim göstermek için zorunlu izin (@capacitor/local-notifications da tanımlar; burada da görünsün) -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <!--
      Hatırlatıcının seçilen saatte gelmesi için gerekli. Bu izin olmadan
      @capacitor/local-notifications setAndAllowWhileIdle (inexact) dalına düşer;
      inexact alarmlar App Standby Bucket'a tabidir ve uygulama bir süre
      açılmayınca saatlerce ertelenir — kullanıcı uygulamayı açtığında hepsi
      birden gelir.

      USE_EXACT_ALARM tercih EDİLMEDİ: o izin kurulumda otomatik verilir ama Play
      yalnızca çekirdek işlevi alarm/takvim olan uygulamalara izin veriyor; zikir
      sayacı bu tanıma girmeyebilir. SCHEDULE_EXACT_ALARM ise kullanıcının
      onayına bağlı, reddedilirse uygulama eski (inexact) davranışa düşer.
    -->
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" />
</manifest>
